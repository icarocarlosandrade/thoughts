server:  servlet:    context-path: /thoughts/apispring:  datasource:    url: jdbc:postgresql://localhost:5432/thoughts # Conexão com o banco de dados    username: postgres    password: 123456  jpa:    properties:      hibernate:        dialect: org.hibernate.dialect.PostgreSQLDialect        format_sql: true    show-sql: false    hibernate:      ddl-auto: none # Desabilita a auto-criação do hibernate (cria as tabelas com base nas entities)  main:    banner-mode: off # Desabilita o banner do spring no output